export const enum USER_STATUS {
    invited = 'invited',
    pending = 'pending',
    confirmed = 'confirmed'
}

export const enum USER_ROLE {
    user = 'user',
    admin = 'admin',
    superadmin = 'superadmin'
}
